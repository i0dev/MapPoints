package com.i0dev.mappoints.cmd;

import com.massivecraft.massivecore.command.MassiveCommand;

public class MapPointsCommand extends MassiveCommand {
    public MapPointsCommand() {
        this.setSetupEnabled(true);
    }
}
